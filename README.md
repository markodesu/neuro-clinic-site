# Neuro Clinic - Система управления клиникой

Веб-приложение для неврологической клиники с чат-ботом, FAQ и системой управления вопросами пациентов.

## 🚀 Технологии

### Backend
- **Node.js** + **Express**
- **Prisma** + **PostgreSQL** (Railway)
- **CORS** для работы с frontend

### Frontend
- **React** (Create React App)
- **Axios** для API запросов
- Деплой на **Vercel**

## 📁 Структура проекта

```
neuro-site/
├── backend/          # Backend API
│   ├── src/
│   │   └── index.js  # Основной сервер
│   └── prisma/       # Prisma схема и миграции
└── frontend/         # React приложение
    └── src/
        ├── components/  # React компоненты
        └── api.js       # Конфигурация API
```

## 🛠️ Установка и запуск

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
Создайте файл `.env`:
```env
DATABASE_URL="postgresql://user:password@host:port/database"
```

4. Примените миграции:
```bash
npx prisma migrate deploy
npx prisma generate
```

5. Запустите сервер:
```bash
npm run dev
```

Сервер будет доступен на `http://localhost:3000`

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите приложение:
```bash
npm start
```

Приложение будет доступно на `http://localhost:3001` (или другом порту)

## 📡 API Endpoints

### Backend API (порт 3000)

- `GET /` - Проверка работы сервера
- `GET /faq` - Получить все активные FAQ
- `POST /ask` - Сохранить вопрос пациента
- `GET /questions` - Получить все вопросы пациентов
- `PATCH /questions/:id/answer` - Добавить ответ врача на вопрос
- `POST /chat` - Чат-бот (проверяет FAQ по ключевым словам)

## 🔧 Конфигурация

### Backend

- **Порт**: 3000
- **База данных**: PostgreSQL (Railway)
- **CORS**: Разрешены все источники

### Frontend

- **Proxy**: `http://localhost:3000` (для локальной разработки)
- **API URL**: Настраивается через переменную окружения `REACT_APP_API_URL`

## 🌐 Деплой

### Backend на Railway

1. Подключите GitHub репозиторий к Railway
2. Выберите папку `backend`
3. Railway автоматически определит Node.js проект
4. Добавьте переменную окружения `DATABASE_URL` (Railway создаст PostgreSQL автоматически)
5. Build Command: `npm install && npx prisma generate && npx prisma migrate deploy`
6. Start Command: `npm start`

**Важно**: Убедитесь, что в `schema.prisma` используется `env("DATABASE_URL")`, а не встроенный URL.

### Frontend на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Import Project → выберите ваш GitHub репозиторий
3. Настройки:
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`
   - **Framework Preset**: Create React App
4. Добавьте переменную окружения:
   - **Name**: `REACT_APP_API_URL`
   - **Value**: `https://your-railway-backend.up.railway.app`
   - **Environment**: Production, Preview, Development

## 🐛 Решение проблем

### Ошибка "Unsupported protocol postgresql:"

Исправлено в коде - автоматическое преобразование `postgresql://` → `postgres://`

### Ошибка "Chat failed" на Vercel

1. Проверьте переменную `REACT_APP_API_URL` в Vercel
2. Убедитесь, что backend доступен публично
3. Проверьте CORS настройки на backend

### Проблемы с базой данных

1. Проверьте `DATABASE_URL` в Railway
2. Убедитесь, что миграции применены: `npx prisma migrate deploy`
3. Проверьте логи на Railway

## 📝 FAQ Keywords

Чат-бот автоматически распознает вопросы по ключевым словам:

- **"где", "адрес"** → "Где вы находитесь?"
- **"записаться", "приём", "прием"** → "Как записаться на приём?"
- **"дни", "график", "работаете"** → "В какие дни работает клиника?"

## 🔐 Переменные окружения

### Backend (.env)
```
DATABASE_URL="postgresql://user:password@host:port/database"
```

### Frontend (Vercel Environment Variables)
```
REACT_APP_API_URL="https://your-backend.up.railway.app"
```

## 📚 Дополнительная документация

- [Prisma Documentation](https://www.prisma.io/docs)
- [Express.js Documentation](https://expressjs.com/)
- [React Documentation](https://react.dev/)
- [Vercel Documentation](https://vercel.com/docs)
- [Railway Documentation](https://docs.railway.app/)

## 👥 Авторы

Проект создан для неврологической клиники.

## 📄 Лицензия

ISC

