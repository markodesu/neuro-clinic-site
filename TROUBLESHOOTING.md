# Решение проблемы "Ошибка сервера" в чате на Vercel

## 🔍 Диагностика проблемы

### 1. Проверьте консоль браузера
Откройте DevTools (F12) → Console и посмотрите:
- Какой URL используется для запроса
- Какая именно ошибка возникает
- Статус код ошибки

### 2. Проверьте переменную окружения в Vercel

**Важно:** Убедитесь, что переменная `REACT_APP_API_URL` установлена:

1. Зайдите на [vercel.com](https://vercel.com)
2. Откройте ваш проект
3. Settings → Environment Variables
4. Проверьте наличие `REACT_APP_API_URL`
5. Значение должно быть: `https://your-railway-backend.up.railway.app`

**После изменения переменных окружения:**
- Передеплойте проект (Redeploy) или подождите автоматического деплоя

### 3. Проверьте доступность backend

Откройте в браузере ваш Railway backend URL:
```
https://your-railway-backend.up.railway.app/
```

Должно вернуться: `Neuro backend is running`

Проверьте эндпоинт:
```
https://your-railway-backend.up.railway.app/chat
```
(Должна быть ошибка метода, но не 404)

### 4. Проверьте CORS на backend

Backend должен разрешать запросы с вашего Vercel домена. 
CORS уже настроен в коде, но убедитесь, что backend перезапущен после изменений.

### 5. Типичные ошибки и решения

#### Ошибка: "Network Error" или "CORS policy"
**Решение:** 
- Убедитесь, что backend запущен и доступен
- Проверьте CORS настройки в backend (уже исправлено в коде)

#### Ошибка: 404 Not Found
**Решение:**
- Проверьте, что `REACT_APP_API_URL` указан правильно
- Убедитесь, что URL не содержит лишних слэшей

#### Ошибка: 500 Internal Server Error
**Решение:**
- Проверьте логи backend на Railway
- Убедитесь, что база данных подключена
- Проверьте, что Prisma миграции применены

## 🛠️ Быстрое исправление

1. **Установите переменную окружения в Vercel:**
   ```
   REACT_APP_API_URL=https://your-railway-backend.up.railway.app
   ```

2. **Передеплойте frontend:**
   - Vercel Dashboard → Deployments → три точки → Redeploy

3. **Проверьте логи:**
   - Vercel Dashboard → Deployments → выберите деплой → Functions Logs
   - Railway Dashboard → Logs (для backend)

## 📝 Проверка после исправления

1. Откройте ваш Vercel сайт
2. Откройте консоль браузера (F12)
3. Попробуйте отправить сообщение в чат
4. Проверьте логи в консоли - должны быть видны:
   - URL запроса
   - Ответ сервера или детали ошибки

